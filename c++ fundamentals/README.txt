C++ fundamentals


Description: (IO, arrays, basic search, print, class definition)
Read up to 100 numbers from a file and store them in a sorted array, then allow the user to see if a specific number has been stored. Read a .csv file and stores the information in a list of structs. Each line of the file corresponds to a struct element in the list. Then you will allow the user to print the contents of the list. Lastly, constructed a simple class to store movie data.
